/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Remove or comment out the experimental.serverActions line
  // experimental: { serverActions: true },
};

export default nextConfig;
